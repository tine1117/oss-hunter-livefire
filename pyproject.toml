[project]
name = "duration-utils"
version = "0.1.0"
description = "Tiny human-friendly duration string parser."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["duration_utils"]
